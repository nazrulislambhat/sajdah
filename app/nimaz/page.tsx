import PrayerTimes from '@/components/nimaz/PrayerTimes';

export default function Nimaz() {
  return (
    <main className="min-h-screen flex justify-center items-center flex-col bg-Lightsajdah py-8">
      <PrayerTimes />
    </main>
  );
}
